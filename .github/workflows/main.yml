name: Somaskkandan

on: push

jobs:
  My-test-job:
    runs-on: ubuntu-latest
    steps:
      - name: message
        run: echo "Hello SK"
        
      - name: install python
        uses: actions/setup-python@v6
        with:
          python-version: '3.10'
      
